import {NextRequest,NextResponse} from 'next/server'; import {createAdminClient} from '@/utils/supabase/server';
const today=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
export async function GET(){const db=createAdminClient();if(!db)return NextResponse.json({error:'Banco indisponível'},{status:503});const date=today();const {data,error}=await db.from('appointments').select('id,start_datetime,price,status,customer_notes,customers(name,phone),barbers(name),services(name,duration_minutes)').eq('appointment_date',date).order('start_datetime');if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({appointments:data,date})}
export async function PATCH(req:NextRequest){const db=createAdminClient();if(!db)return NextResponse.json({error:'Banco indisponível'},{status:503});const {id,status}=await req.json();const allowed=['aguardando confirmação','confirmado','concluído','cancelado','não compareceu'];if(!id||!allowed.includes(status))return NextResponse.json({error:'Dados inválidos'},{status:400});const {error}=await db.from('appointments').update({status,updated_at:new Date().toISOString()}).eq('id',id);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true})}
